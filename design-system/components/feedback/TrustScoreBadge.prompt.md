Shows a customer's reliability score in the order form (Module 1 — CRM). `locked` renders the red "khóa COD" state used for strangers with no order history.

```jsx
<TrustScoreBadge score={2} locked />
<TrustScoreBadge score={5} />
```
