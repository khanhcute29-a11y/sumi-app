Labeled text field for order forms (e.g. "Tên khách", "Mã Môi Giới"). Shows an inline error message in brand red when `error` is set.

```jsx
<Input label="Mã Môi Giới (Broker ID)" placeholder="VD: BR-0182" />
<Input label="SĐT khách hàng" error="Số điện thoại không hợp lệ" />
```
