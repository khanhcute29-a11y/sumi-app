Replaces `Sidebar` below the tablet breakpoint. Exactly the 4 KiotViet-style core icons the brief specifies, plus a "Thêm" (More) button that should open a popup menu with the remaining features.

```jsx
<BottomNav active="orders" onSelect={setTab} onMore={openMoreSheet} />
```
