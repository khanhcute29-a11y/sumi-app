Fixed left navigation for Desktop/Laptop layouts per the CRITICAL RESPONSIVE LAYOUT RULE — hidden below the tablet breakpoint in favor of `BottomNav`.

```jsx
<Sidebar active="orders" onSelect={setTab} />
```
