Underline tab bar. Used for the Cashbook's 4 tabs (THU / CHI / CÔNG NỢ / P&L), the kitchen's stations (Bánh / Đồ uống / Dụng cụ), and inbox filters (Tất cả / Chờ cọc / Bếp đang làm / VIP).

```jsx
<Tabs tabs={[{key:'thu',label:'THU'},{key:'chi',label:'CHI'}]} active="thu" onChange={setTab} />
```
